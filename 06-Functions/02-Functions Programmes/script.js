/**
 * 🟢 **JavaScript Functions: 10 Easy Examples** 🟢
 *
 * ──── 🌟 **1. Greet Someone** 🌟 ────
 *
 * A function that greets a person by name. 👋
 *
 * ```javascript
 *
 * function greet(name) {
 *   console.log("Hello, " + name + "! 👋");
 * }
 * greet("Alice");  // Output: Hello, Alice! 👋
 * greet("Bob");    // Output: Hello, Bob! 👋
 * ```
 *
 * ──── 🌟 **2. Add Two Numbers** 🌟 ────
 *
 * A function that adds two numbers. ➕
 *
 * ```javascript
 * function add(a, b) {
 *   return a + b;
 * }
 * var result = add(5, 3);  // Output: 8
 * console.log(result);
 * ```
 *
 * ──── 🌟 **3. Multiply Two Numbers** 🌟 ────
 *
 * A function that multiplies two numbers. ✖️
 *
 * ```javascript
 * function multiply(a, b) {
 *   return a * b;
 * }
 * var result = multiply(4, 6);  // Output: 24
 * console.log(result);
 * ```
 *
 * ──── 🌟 **4. Subtract Two Numbers** 🌟 ────
 *
 * A function that subtracts one number from another. ➖
 *
 * ```javascript
 * function subtract(a, b) {
 *   return a - b;
 * }
 * var result = subtract(10, 4);  // Output: 6
 * console.log(result);
 * ```
 *
 * ──── 🌟 **5. Find the Maximum Number** 🌟 ────
 *
 * A function that finds the bigger number between two numbers. 🔝
 *
 * ```javascript
 * function findMax(a, b) {
 *   return a > b ? a : b;
 * }
 * var max = findMax(7, 9);  // Output: 9
 * console.log(max);
 * ```
 *
 * ──── 🌟 **6. Calculate the Area of a Rectangle** 🌟 ────
 *
 * A function to calculate the area of a rectangle. 📏
 *
 * ```javascript
 * function calculateArea(length, width) {
 *   return length * width;
 * }
 * var area = calculateArea(5, 3);  // Output: 15
 * console.log(area);
 * ```
 *
 * ──── 🌟 **7. Check if a Number is Even or Odd** 🌟 ────
 *
 * A function that checks if a number is even or odd. 🔢
 *
 * ```javascript
 * function checkEvenOdd(number) {
 *   if (number % 2 === 0) {
 *     console.log(number + " is even! 🎉");
 *   } else {
 *     console.log(number + " is odd! 😎");
 *   }
 * }
 * checkEvenOdd(4);   // Output: 4 is even! 🎉
 * checkEvenOdd(9);   // Output: 9 is odd! 😎
 * ```
 *
 * ──── 🌟 **8. Calculate the Factorial of a Number** 🌟 ────
 *
 * A function that calculates the factorial of a number. 🔢
 *
 * ```javascript
 * function factorial(n) {
 *   if (n === 0 || n === 1) {
 *     return 1;
 *   }
 *   return n * factorial(n - 1);
 * }
 * var result = factorial(5);  // Output: 120
 * console.log(result);
 * ```
 *
 * ──── 🌟 **9. Convert Celsius to Fahrenheit** 🌟 ────
 *
 * A function that converts Celsius to Fahrenheit. 🌡️
 *
 * ```javascript
 * function celsiusToFahrenheit(celsius) {
 *   return (celsius * 9/5) + 32;
 * }
 * var fahrenheit = celsiusToFahrenheit(25);  // Output: 77
 * console.log(fahrenheit);
 * ```
 *
 * ──── 🌟 **10. Concatenate Strings** 🌟 ────
 *
 * A function that concatenates (joins) two strings. 📝
 *
 * ```javascript
 * function concatenate(str1, str2) {
 *   return str1 + " " + str2;
 * }
 * var sentence = concatenate("Hello", "world!");  // Output: Hello world!
 * console.log(sentence);
 * ```
 *
 * ──── 📢 **Key Takeaways** 📢 ────
 *
 * 1️⃣ Functions allow you to reuse code and make it **cleaner**. 🔄
 * 2️⃣ You can use **parameters** to make your functions flexible. 🛠️
 * 3️⃣ **Return** values from functions to get results later. 🔙
 * 4️⃣ Play around with these examples to improve your coding skills! 🎮
 *
 * Functions = **Simpler Code** ⚡. Keep practicing! 💻✨
 */


// Keep Partcing 
